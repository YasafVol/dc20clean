/**
 * Canonical content provenance tags for rules data.
 * These tags are metadata only and do not enforce access by themselves.
 */
export type ContentTag = 'SRD' | 'MAGAZINE';

export const CONTENT_TAGS: ContentTag[] = ['SRD', 'MAGAZINE'];
